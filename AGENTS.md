# Repository Rules

## Security

- This repository is open source; avoid asking users to enter secrets or sensitive personal information.

## Design System Sync

- When you update a UI component or layout in `App.tsx`, apply the same change to the corresponding mock in `DesignDemo.tsx`, and vice versa.
